(use-modules (App)
	     ((dom) #:select (insert-adjacent-HTML get-element-by-id))
	     ((htmlprag) #:select (shtml->html shtml-normalize)))

(insert-adjacent-HTML
 (get-element-by-id "app")
 "beforeEnd"
 (shtml->html (shtml-normalize ((App 'shtml)))))

((App 'event))
