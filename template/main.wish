(use-modules (App)
	     ((dom) #:select (insert-adjacent-HTML get-element-by-id))
	     ((htmlprag) #:select (shtml->html)))

(insert-adjacent-HTML
 (get-element-by-id "app")
 "beforeEnd"
 (shtml->html (App)))
